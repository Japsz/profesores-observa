<%if(is_login){%>
	<ul class="nav navbar-nav navbar-right" style="height: 50px">
	    <li>
	    	<a href="/teacher/info_teacher" style="padding: 0;">
	    		<img src="<%=data.perfil_image%>" class="img-circle" style="width: 50px; height: 50px; padding: 3px; margin-right: <%=data.username.length+1 %>0px">
	    		<h2 class="vertical_center" style="left: 55px; margin: 0; font-size: 20px"><%=data.username %></h2>
	    	</a>
	    </li>
	    <li>
	    	<!-- Notificaciones -->
	    	<div class="dropdown" style="margin-left: 5px; top: 5px">
                <button class="dropbtn btn" style="padding: 0; margin: 0">
                    <h4 class="vertical_center" style="left: 44%; font-weight: 600; color: black">
                    	<div id="nro_notifications" data-nro="0">0</div>
                	</h4>
                    <img src="/icons/RRCC web-27.png" class="img-rounded" style="width: 50px;">
                </button>
                <div id="notifications" class="dropdown-content-notif">

                </div>
            </div>
	    </li>
	    <li>
	    	<a href="/teacher/logout" class="btn" style="padding: 0;">
	    		<img src="/icons/RRCC web-28.png" class="img-rounded" style="width: 30px; margin: 7px">
	    	</a>
	    </li>
	</ul>

	<script type="text/javascript">
		$(document).ready(function(){
			var teacher_list = [];
            $.ajax({
                type: 'POST',
                data: JSON.stringify({teacher_list: teacher_list}),
                contentType: 'application/json',
                url: '/teacher/add_notification',
                success: function(data){
                	if(data != "ignore"){
	                    $("#notifications").html(data);
	                    $("#nro_notifications").html($("#data_length").data("length"));
						$("#nro_notifications").data("nro", $("#data_length").data("length"));
                	}
                }
            });
        });
	</script>

<%}else{%>
	<ul class="nav navbar-nav navbar-right">
	    <li>
	    	<h4 style="padding-top: 10%">
		    	<a href="/teacher/login_teacher" style="color: white; text-decoration: none">Iniciar Sesión <i class="fa fa-sign-in"></i>
		    	</a>
	    	</h4>
	    </li>
	</ul>
<%}%>

<style type="text/css">
	.dropdown-content-notif {
		display: none;
		position: absolute;
		right: 0;
		background-color: #f9f9f9;
		min-width: 240px;
		box-shadow: 0px 8px 16px 0px black;
		z-index: 1;
    }
    .dropdown-content-notif a {
		color: black;
		padding: 12px 16px;
		text-decoration: none;
		display: block;
    }
    .dropdown-content-notif a:hover {
        background-color: #ccc;
    }
    .dropdown:hover .dropdown-content-notif {
        display: block;
    }
</style>